import GlobalEnum from '../../models/GlobalEnum';

function InterfaceMain() {
  let a = GlobalEnum.defaultValue;

  return (
    <div>
        main {a}
    </div>
  );
}

export default InterfaceMain;
